# Superpowers Library Rules

These rules apply only when working inside `libs/superpowers/`.

1. Keep changes scoped to `libs/superpowers/` unless explicitly required.
2. Do not apply rules from other libraries in this monorepo.
3. If a task spans multiple libraries, confirm scope before editing outside this folder.
